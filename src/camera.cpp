#include "esp_camera.h"
#include <WiFi.h>
#include "config.h" // Include the header for the shared variables

void setupCamera() {
  // Camera-specific setup code here
  Serial.println("Setting up the camera...");
}

void startCameraServer() {
  // Your camera server code here
}
